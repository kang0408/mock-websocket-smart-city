{
  "name": "mock-websocket-server",
  "version": "1.0.0",
  "description": "A simple mock websocket server.",
  "main": "mock-websocket-server.js",
  "scripts": {
    "start": "node mock-websocket-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "websocket",
    "mock"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.3"
  }
}
